# ReMask clean preview

This branch is an isolated preview build of the clean ReMask codebase. It does not replace production v11.2. The runtime source is packed into `remask-preview-runtime.tar.gz` only as a transport mechanism for the preview deployment; production source-of-truth will be migrated to a normal file tree after preview validation.
